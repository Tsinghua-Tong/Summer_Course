# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
.vscode/
.idea/

# Note: site/index.html is generated by site/build.py. It is committed on purpose so the
# shareable page is available without a build step. Run `python site/build.py` after editing
# any content/*.md, then commit the regenerated file.
